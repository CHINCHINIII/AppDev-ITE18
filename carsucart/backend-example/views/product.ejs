<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %> | CarSUcart</title>
    <!-- Using Tailwind CSS via CDN for demonstration -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom scrollbar for better aesthetics */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #f1f1f1; }
        ::-webkit-scrollbar-thumb { background: #888; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #555; }
    </style>
</head>
<body class="bg-white text-slate-800">

    <!-- Navbar Placeholder -->
    <nav class="border-b py-4 px-6 mb-8 sticky top-0 bg-white/95 backdrop-blur z-50">
        <div class="max-w-7xl mx-auto font-bold text-xl text-green-600">CarSUcart</div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 pb-16">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
            
            <!-- 
               1. IMAGE SECTION FIX 
               - Used h-[70vh] to keep image within viewport height.
               - Used object-contain to ensure the whole image is visible without cropping.
               - Used sticky top-24 to keep it in view while scrolling description/reviews.
            -->
            <div class="sticky top-24 h-fit">
                <!-- Main Image Container -->
                <div class="h-[70vh] bg-gray-50 rounded-2xl overflow-hidden mb-4 border border-gray-100 flex items-center justify-center relative group">
                    <img 
                        src="<%= product.image %>" 
                        alt="<%= product.name %>" 
                        class="w-full h-full object-contain transition-transform duration-300 group-hover:scale-105"
                    >
                </div>
                
                <!-- Thumbnails -->
                <div class="grid grid-cols-4 gap-4">
                    <button class="aspect-square rounded-xl border-2 border-green-500 overflow-hidden bg-gray-50">
                        <img src="<%= product.image %>" class="w-full h-full object-cover">
                    </button>
                    <% product.thumbnails.forEach(thumb => { %>
                        <button class="aspect-square rounded-xl border-2 border-transparent hover:border-gray-300 overflow-hidden bg-gray-50">
                            <img src="<%= thumb %>" class="w-full h-full object-cover">
                        </button>
                    <% }) %>
                </div>
            </div>

            <!-- 
               2. PRODUCT INFO & ORDERING FLOW 
               - Description moved to tabs below to keep ordering buttons high up.
               - Stock, Variant, Quantity, Buttons are prioritized here.
            -->
            <div class="flex flex-col h-full">
                <!-- Header -->
                <div class="mb-6">
                    <span class="inline-block bg-green-100 text-green-700 px-3 py-1 rounded-full text-sm font-medium mb-3">
                        <%= product.category %>
                    </span>
                    <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-2"><%= product.name %></h1>
                    <div class="flex items-center gap-2 text-sm text-gray-600">
                        <div class="flex text-yellow-400">★★★★★</div> 
                        <span><%= product.rating %> (<%= product.reviewsCount %> reviews)</span>
                        <span>•</span>
                        <span class="text-green-600 font-medium"><%= product.seller %></span>
                    </div>
                </div>

                <!-- Price -->
                <div class="text-3xl font-bold text-green-600 mb-8">
                    <%= product.currency %> <%= product.price.toLocaleString() %>
                </div>

                <!-- Ordering Controls -->
                <div class="space-y-6">
                    <!-- Variants -->
                    <% if (product.variants && product.variants.length) { %>
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Select Variant</label>
                        <div class="flex flex-wrap gap-2">
                            <% product.variants.forEach((variant, index) => { %>
                                <button class="px-4 py-2 border rounded-lg hover:border-green-500 hover:text-green-600 <%= index === 0 ? 'border-green-500 bg-green-50 text-green-700' : 'border-gray-200' %>">
                                    <%= variant %>
                                </button>
                            <% }) %>
                        </div>
                    </div>
                    <% } %>

                    <!-- Quantity & Stock -->
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Quantity</label>
                        <div class="flex items-center gap-4">
                            <div class="flex items-center border border-gray-300 rounded-lg">
                                <button class="px-3 py-2 hover:bg-gray-100 text-gray-600">-</button>
                                <input type="text" value="1" class="w-12 text-center border-none focus:ring-0" readonly>
                                <button class="px-3 py-2 hover:bg-gray-100 text-gray-600">+</button>
                            </div>
                            <span class="text-sm text-gray-500"><%= product.stock %> items available</span>
                        </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="flex gap-4 pt-4 border-t border-gray-100 mt-auto">
                        <button class="flex-1 px-6 py-4 border-2 border-green-600 text-green-600 font-bold rounded-xl hover:bg-green-50 transition-colors flex items-center justify-center gap-2">
                            <span>Add to Cart</span>
                        </button>
                        <button class="flex-1 px-6 py-4 bg-green-600 text-white font-bold rounded-xl hover:bg-green-700 shadow-lg shadow-green-200 transition-all flex items-center justify-center gap-2">
                            <span>Buy Now</span>
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- 
           3. TABS SECTION
           - Description and Reviews placed here to avoid pushing down order buttons.
        -->
        <div class="mt-16">
            <div class="border-b border-gray-200">
                <nav class="-mb-px flex gap-8">
                    <a href="#" class="border-b-2 border-green-500 py-4 px-1 text-sm font-medium text-green-600">
                        Description
                    </a>
                    <a href="#" class="border-b-2 border-transparent py-4 px-1 text-sm font-medium text-gray-500 hover:text-gray-700 hover:border-gray-300">
                        Reviews (<%= product.reviewsCount %>)
                    </a>
                </nav>
            </div>

            <div class="py-8 prose max-w-3xl text-gray-600 leading-relaxed">
                <p><%= product.description %></p>
                
                <h3 class="text-gray-900 font-bold mt-8 mb-4">Product Features</h3>
                <ul class="list-disc pl-5 space-y-2">
                    <li>Premium build quality</li>
                    <li>Verified authentic product</li>
                    <li>Fast shipping available</li>
                </ul>
            </div>
        </div>

    </main>

</body>
</html>
